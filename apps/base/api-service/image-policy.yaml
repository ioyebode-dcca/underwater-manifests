apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: api-sevice
  namespace: flux-system
spec:
  image: 495905914919.dkr.ecr.us-east-1.amazonaws.com/api-service
  interval: 1m
  provider: aws
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: api-service
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: api-service
  filterTags:
    pattern: "^dev-([0-9]+)$"
    extract: "$1"
  policy:
    numerical:
      order: asc